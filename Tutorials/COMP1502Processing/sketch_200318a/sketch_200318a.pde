

// sets the size of the processing window
size(400, 400);
  // width x height
  
  // sizes are in pixels (one dot on the screen)
  
// the x,y grid starts in the top left  
  
background(255);
// monochrome - just one arg (0 is black 255 is white)

fill(123, 200, 79);
// red green blue values for the colour
ellipse(0, 0, 80, 80);

fill(200, 81, 79);
rect( 180, 180, 40, 40);

stroke(88, 120, 200);
line(100,100,300,300);
